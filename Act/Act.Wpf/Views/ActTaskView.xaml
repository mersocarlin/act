<Window x:Class="Act.Wpf.Views.ActTaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task" Height="145" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                <TextBlock Text="Title" Width="70" TextAlignment="Right" Margin="0 0 10 0"></TextBlock>
                <TextBox Text="{Binding Title}" Width="280" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                <TextBlock Text="Description" Width="70" TextAlignment="Right" Margin="0 0 10 0"></TextBlock>
                <TextBox Text="{Binding Description}" Width="280" />
            </StackPanel>

            <Button Content="Save" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
